# 项目词汇表

## 构建控制仓库

用于描述和触发 OpenWrt 固件构建的轻量仓库。它只保存构建规则、固件配置、校验脚本和项目说明，不保存上游源码或构建产物。

## 上游源码

OpenWrt 官方项目的源码。上游源码是固件构建的输入来源，但不是本项目的一部分。

## 固件配置

描述目标平台、镜像类型和预装能力的配置。当前项目只维护一份固件配置，作为所有构建的共同输入。

## 目标平台

固件面向的设备类型。当前项目的目标平台固定为 x86_64 软路由设备。

## 软路由固件

面向通用 x86_64 设备的 OpenWrt 固件，重点支持多网口、基础路由、LuCI 管理、诊断工具、存储维护、WireGuard、SQM、流量统计和 banIP 能力。

## 固件产物

构建完成后可刷写或部署的镜像文件。当前阶段固件产物通过 GitHub Actions Artifacts 交付。

## 诊断产物

用于排查构建配置和固件内容的辅助文件集合，例如最终配置、manifest 和 buildinfo。诊断产物不等同于可部署固件。

## 运行时包

预装进固件并可在设备运行时使用的软件包。运行时包必须通过 manifest 校验确认存在。

## 构建缓存

用于复用下载内容、编译缓存和中间构建目录的数据。构建缓存的目标是缩短后续构建时间，但不改变固件功能。

## 配置校验

确认请求的固件配置没有在配置归一化过程中被意外移除的检查。配置校验失败表示配置声明和最终构建输入不一致。

## Manifest 校验

确认关键运行时包实际进入固件的检查。Manifest 校验失败表示固件产物不满足预装功能要求。

## 第三方软件包源

OpenWrt 官方 feeds 之外的软件包来源。第三方软件包源只在构建时拉取，是固件构建的输入，不属于本项目仓库内容。

## 代理服务插件栈

围绕代理服务管理界面及其运行时后端组成的一组预装能力。当前项目使用 SSR-Plus 作为代理服务插件栈的主要 LuCI 入口。

## MosDNS 插件栈

围绕 MosDNS 管理界面、MosDNS 运行时程序和地理数据转换工具组成的一组预装能力。它用于在软路由固件中提供可配置的 DNS 分流和转发能力。

## 上游包覆盖

当多个上游来源提供同名 OpenWrt 包时，项目显式选择其中一个来源并屏蔽其他来源的做法。上游包覆盖用于避免重复包定义导致构建结果不确定。
